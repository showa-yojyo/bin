#!/bin/bash

# Rename files sequentially

set -o errexit
set -o nounset
set -o pipefail

declare -ar input=(${@})
declare -ir num_video=${#input[@]}

for (( i=0; i<$num_video; i++ ));
do
    extension="${input##*.}"
    mv "${input[i]}" ${i}."${extension}"
done
